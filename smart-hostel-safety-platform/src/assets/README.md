# Assets

Place static assets here (images, icons, fonts, etc.). None of the current
UI uses local image assets — everything is built with Tailwind utility
classes and `lucide-react` icons — so this folder is currently empty.
